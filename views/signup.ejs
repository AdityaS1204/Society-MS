<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Register</title>
    <link rel="stylesheet" href="/css/signup.css" />
  </head>

  <body>
    <div class="container">
      <h1>Let's set up your ResidentLINK account</h1>
      <p>
        Tell us more about yourself so we can create a personalized experience
        tailored to your needs.
      </p>

      <!-- Profile Picture Upload -->
       <div class="profile-picture">
        <img
          src="https://upload.wikimedia.org/wikipedia/commons/9/99/Sample_User_Icon.png"
          alt="Profile Picture"
          id="profilePicPreview"
        />
        <div class="upload-btn-wrapper">
          <button class="upload-btn">Upload Image</button>
          <input
            type="file"
            id="profilePicture"
            accept="image/*"
            name="profileImageURL"
            onchange="previewProfilePic(event)"
          />
        </div>
        <small class="file-hint"
          >.png, .jpeg, .jpg files up to 8MB. Recommended size: 256x256px</small
        >
      </div> 

      <!-- User Details Form -->
      <form
        class="user-details-form"
        id="userDetailsForm"
        action="/user/signup"
        method="post"
      >
        <div class="form-row">
          <label for="firstName">First Name</label>
          <input type="text" id="firstName" name="firstName" placeholder="First Name" required />
        </div>

        <div class="form-row">
          <label for="lastName">Last Name</label>
          <input type="text" id="lastName" name="lastName" placeholder="Last Name" required />
        </div>

        <div class="form-row full-width">
          <label for="email">Email</label>
          <input type="email" id="email" name="email" placeholder="Email" required />
        </div>

        <div class="form-row full-width">
          <label for="phone">Phone No.</label>
          <input type="tel" id="phone" name="Phone" placeholder="Phone Number" required />
        </div>

        <div class="form-row full-width">
          <label for="address">Address</label>
          <input type="text" id="address" name="address" placeholder="Address" required />
        </div>

        <div class="form-row">
          <label for="pincode">Pincode</label>
          <input type="text" id="pincode" name="pincode" placeholder="Pincode" required />
        </div>

        <div class="form-row">
          <label for="gender">Gender</label>
          <select id="gender" name="gender" required>
            <option value="">Select Gender</option>
            <option value="male">Male</option>
            <option value="female">Female</option>
            <option value="other">Other</option>
          </select>
        </div>

        <div class="form-row">
          <label for="role">Role</label>
          <select id="role" name="role" required>
            <option value="user">User</option>
            <option value="Admin">Admin</option>
          </select>
        </div>

        <div class="form-row">
          <label for="occupation">Occupation</label>
          <input
            type="text"
            id="occupation"
            placeholder="Occupation"
            name="occupation"
            required
          />
        </div>

        <div class="form-row">
          <label for="familyMembers">Family Members</label>
          <select id="familyMembers" name="familyMembers" required>
            <option value="">Select</option>
            <option value="1-2">1-2</option>
            <option value="3-4">3-4</option>
            <option value="5+">5+</option>
          </select>
        </div>

        <div class="form-row full-width">
          <label for="password">Password</label>
          <input
            type="password"
            id="password"
            placeholder="Password"
            name="password"
            required
          />
        </div>

        <!-- Separation Line -->
        <!-- <hr class="section-separator" />

        <!-- Document Upload Section -->
        <!-- <h1>Upload Your Documents</h1>
        <p class="subtext">
          Each file should not exceed 5MB. Allowed formats: JPG, PNG, PDF.
        </p> -->

        <!-- <div class="document-upload">
          <!-- Aadhar Card Upload Section -->
          <!-- <div class="upload-card">
            <h3>Aadhar Card</h3>
            <div class="upload-wrapper">
              <input
                type="file"
                id="aadhar"
                accept="image/*,.pdf"
                onchange="validateFile(this)"
              />
              <button
                class="view-btn"
                id="viewAadhar"
                onclick="viewImage('aadhar')"
              >
                View
              </button>
            </div>
            <small class="error-message" id="aadharError"></small>
          </div>

          <!-- PAN Card Upload Section -->
          <!-- <div class="upload-card">
            <h3>PAN Card</h3>
            <div class="upload-wrapper">
              <input
                type="file"
                id="pan"
                accept="image/*,.pdf"
                onchange="validateFile(this)"
              />
              <button class="view-btn" id="viewPan" onclick="viewImage('pan')">
                View
              </button>
            </div>
            <small class="error-message" id="panError"></small>
          </div> -->

          <!-- Voter ID Upload Section -->
          <!-- <div class="upload-card">
            <h3>Voter ID</h3>
            <div class="upload-wrapper">
              <input
                type="file"
                id="voter"
                accept="image/*,.pdf"
                onchange="validateFile(this)"
              />
              <button
                class="view-btn"
                id="viewVoter"
                onclick="viewImage('voter')"
              >
                View
              </button>
            </div>
            <small class="error-message" id="voterError"></small>
          </div> -->

          <!-- Passbook Upload Section -->
          <!-- <div class="upload-card">
            <h3>Passbook</h3>
            <div class="upload-wrapper">
              <input
                type="file"
                id="passbook"
                accept="image/*,.pdf"
                onchange="validateFile(this)"
              />
              <button
                class="view-btn"
                id="viewPassbook"
                onclick="viewImage('passbook')"
              >
                View
              </button>
            </div>
            <small class="error-message" id="passbookError"></small>
          </div>
        </div> --> 

        <!-- Modal for viewing images -->
        <!-- Modal for viewing images -->
        <!-- <div class="modal" id="imageModal">
          <div class="modal-content">
            <span class="close-btn" onclick="closeModal()">&times;</span>
            <!-- Close button (X) -->
            <!-- <img id="modalImage" src="#" alt="Document Preview" />
          </div>
        </div>  --> 

        <!-- Submit Button -->
        <button type="submit" class="submit-btn" id="submitBtn" >
          Create Account
        </button>
      </form>
    </div>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.11.0/gsap.min.js"></script>
    <!-- <script src="/js/signup.js"></script> -->
  </body>
</html>
